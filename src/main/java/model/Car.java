package model;

/**
 * Car data model for storing Car data
 */
public class Car extends Vehicle {
    public Car(String registrationNumber, Driver driver) {
        super(registrationNumber, driver);
    }
}
